name: "[deprecated] Build Android APK — use android-build.yml instead"

on:
  workflow_call: {}

jobs:
  deprecated:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "This stub is intentionally never triggered. The real signed AAB+APK build lives in .github/workflows/android-build.yml ('Build Android AAB + APK'). Run that one from the Actions tab."
