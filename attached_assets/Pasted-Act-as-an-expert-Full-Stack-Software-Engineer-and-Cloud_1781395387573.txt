Act as an expert Full-Stack Software Engineer and Cloud Architect. Build a full-blown, production-ready Enterprise Cloud Infrastructure Admin Dashboard Control Center using Node.js with Express for the backend server and vanilla HTML5, CSS3 (leveraging Tailwind CSS for clean layout grid scaling), and modern JavaScript (ES6+) for the frontend UI. The system must operate as a unified, highly optimized single-page administrative dashboard interface that establishes a secure backend server proxy to communicate directly with Microsoft Azure's REST APIs, completely hiding all sensitive cloud credentials from the public browser inspector. The application backend must initialize authentication using the official @azure/identity package with the ClientSecretCredential method. Hardcode placeholder initialization variables at the top of the backend server file using the specific active credentials we generated: Tenant ID 4030dc9c-e588-42d2-8539-6be1c1f927ce, Application Client ID 708de6eb-cfb7-4b56-83eb-73e1bea05af7, and placeholder strings for the Client Secret Value, Subscription ID, Resource Group Name (turboanswergroup), and target App Service Web App name. Ensure the backend code handles automatic OAuth2 token acquisition and silent background token renewal without dropping requests. The frontend user interface must feature a sophisticated, ultra-high-density, professional enterprise admin dashboard layout. Every single square inch of the viewport must be packed with active telemetry grids, data tables, contextual dropdown selectors, utility configurations, and an overwhelming wall of actionable buttons. The visual styling must strictly reject bright, playful, or basic primary colors. Instead, utilize a sleek, dark industrial color palette consisting of a deep charcoal and navy background (#0b0f19), slate grays for borders, sharp ice-blue for active text streams, deep emerald green for normal operational states, dark amber for system warnings, and muted crimson for critical destructive actions. Create a prominent "Infrastructure Operations Management" panel featuring an aggressive array of hyper-responsive system command buttons. Implement standalone frontend UI buttons styled cleanly with professional typography and icon accents labeled "EMERGENCY STOP APPLICATION SERVICE", "COLD START SYSTEM BOOT", "FORCE HARD SERVICE RESTART", "ENTER DEEP SLEEP POWER MODE", and "TRIGGER PRODUCTION SLOT SWAP". Each button must immediately emit an AJAX POST fetch request to specialized backend routes (/api/control/[action]) which construct and execute the precise Azure Resource Manager HTTP requests targeting the App Service site management endpoints using the api-version=2026-03-15 specifications. Build a massive, wide-screen scrolling log monitor console window that mimics an enterprise-grade log stream with monospace fonts. The backend must continuously poll the Azure Monitor Logs API using Kusto Query Language (KQL) to fetch records from the AzureActivity table inside the Log Analytics Workspace. The system must output a high-frequency grid filtering for TimeGenerated, OperationNameValue, Caller, and ActivityStatusValue. Color-code the rows dynamically with sophisticated status tints: failed operations like ValidateSupportPlanPurchase or access denials must display in clean dark-red text accents, while successful writes or network pings scroll quietly in muted gray-green. Integrate a dedicated hardware performance visualization matrix that tracks live server health by communicating with the Azure Monitor Metrics API. Use a lightweight canvas charting library like Chart.js or ApexCharts styled with thin lines and subtle gradient fills to render real-time, side-by-side moving graphs for three critical infrastructure variables over a rolling 1-hour timeline with 1-minute granularity: "Percentage CPU Utilization", "Memory In-Use Bytes", and "Network Data Ingress/Egress Bandwidth Throughput". Add manual configuration knobs right above the charts allowing users to click buttons to change timelines from 1 hour to 15 minutes or a full 24 hours instantly. Implement a deep user-analytics panel powered by mock or live Azure Application Insights instrumentation streams to give instant visibility into web app operations. This panel must contain multi-column mini-grids displaying real-time metrics including "Active Concurrent User Sessions Count", "Average Page-Load Latency (ms)", "Dependency API Response Latency Graphs", and a breakdown of "HTTP Client Response Status Codes" showing a live ratio bar of 2xx Success, 4xx Bad Requests, and 5xx Server Faults. Each metric widget must include individual "Refresh Stream", "Freeze Graph", and "Download CSV Logging Data" utility buttons grouped neatly side-by-side. Construct a highly visible "Financial Operations & Cost Guard" management console that pulls structural spend telemetry via the Azure Consumption REST API. The panel must display clean corporate monetary counters showing "Accrued Month-to-Date Spend ($)", "AI-Predicted End-of-Month固定 Forecast ($)", and a broken-down donut chart segmenting costs by resource categories (Hosting, Log Analytics Storage, Database Transactions). Next to this data, pack a dangerous array of financial control action buttons: "DEFINE STRICT DAILY COST LIMIT CAP", "EMERGENCY SUSPEND ALL OVER-BUDGET RESOURCES", and "TRIGGER BILLING COMPLIANCE AUDIT REPORT". Expand the absolute control density of the command center by introducing an isolated panel specifically dedicated to backend state and database system manipulation. Provide explicit functional action buttons to interact with connected database infrastructures: "PURGE ALL TRANSIENT APPLICATION CACHE REPLICAS", "TRIGGER MANUAL FULL-SYSTEM SQL ENCRYPTED BACKUP", "RE-INDEX PRIMARY DATABASE SCHEMAS", "KILL IDLE ACTIVE DATABASE POOL CONNECTIONS", and "FORCE RE-GENERATE SECONDARY CONNECTION ACCESS STRINGS". Ensure every single operation updates a visual progress bar or terminal feedback message confirming Azure's background job execution sequence status. Because the system will contain highly powerful, destructive, and intimidating control buttons, implement a global security system to prevent accidental misclicks. For high-risk destructive buttons (like Stop, Restart, Cache Purge, and Budget Suspend), do not execute the action immediately; instead, require a dual-action interface workflow. Clicking the button must temporarily swap out its text, turning it into a flashing warning box reading "CONFIRM ACTION within 3 seconds" alongside a small countdown progress bar. Ensure the frontend has a global "Lock Admin Operations" master toggle switch at the absolute top corner of the web application layout to instantly render every control button inert.Let me know if you are ready to run this in Replit, or if you want me to provide the JSON code structure that your backend will receive from Azure!AI responses may include mistakes. Learn more